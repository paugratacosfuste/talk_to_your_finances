// Placeholder home page — replace this with your feature components.
// Data layer is ready: import { useFinancialData } from '@/hooks/useFinancialData'

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-gray-900 mb-4">
          Talk to Your Finances
        </h1>
        <p className="text-gray-500 text-lg">
          Foundation layer ready. Start building your features!
        </p>
      </div>
    </main>
  );
}
